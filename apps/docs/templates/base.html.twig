<!DOCTYPE html>
<html lang="{{ app.locale|default('en') }}" class="bg-gray-900">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {% set seo_title_content %}
        {% block seo_title %}
            HarmonyUI - Modern UI Component Library for Symfony
        {% endblock %}
    {% endset %}
    {% set seo_description_content %}
        {% block seo_description %}
            Build stunning Symfony applications with HarmonyUI. Modern component library featuring beautiful Twig components, TailwindCSS styling, and Stimulus controllers. Free, open-source, and developer-friendly.
        {% endblock %}
    {% endset %}
    {% set seo_keywords_content %}
        {% block seo_keywords %}
            symfony component library, twig components, tailwindcss, stimulus, php ui library, symfony ui kit, web components, modern ui design
        {% endblock %}
    {% endset %}

    {%- set ogImage -%}
        {%- block og -%}
            {{ absolute_url(asset('og.png')) }}
        {%- endblock -%}
    {%- endset -%}

    {{ include('./seo/_default.html.twig') }}
    {{ include('./seo/_og.html.twig') }}
    {{ include('./seo/_twitter.html.twig') }}

    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css"/>

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

    {{ include('./integrations/ga4.html.twig') }}
    {{ include('./integrations/onedollarstats.html.twig') }}
    {{ include('./integrations/clarity.html.twig') }}
</head>
<body class="bg-gray-900 dark:text-white">
    <twig:Layout:Navbar/>
    <main class="isolate relative overflow-clip rounded-b-4xl flex flex-col bg-white dark:bg-gray-900 z-10">
        {% block body %}{% endblock %}
    </main>
    <twig:Layout:Footer/>
</body>
</html>
