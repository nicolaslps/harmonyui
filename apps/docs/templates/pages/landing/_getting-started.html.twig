<section class="py-24 bg-primary/50">
    <div class="relative max-w-6xl mx-auto px-6 text-center">
        <h2 class="text-4xl font-bold text-gray-900 sm:text-5xl mb-8 text-balance">
            Get started in minutes
        </h2>

        <div class="mb-16 max-w-2xl mx-auto">
            <div class="bg-gray-900 border border-gray-700 rounded-xl p-8">
                <div class="flex items-center justify-between mb-4">
                    <span class="text-sm font-medium text-gray-400">Terminal</span>
                </div>
                <pre class="text-left text-lg"><code class="text-primary font-mono">composer require stay/tuned</code></pre>
            </div>
        </div>

        <div class="grid md:grid-cols-3 gap-8 mb-16">
            <div class="bg-white p-8 border border-gray-200 rounded-xl hover:border-primary hover:shadow-lg transition-all duration-300">
                <div class="w-16 h-16 bg-primary text-primary rounded-xl flex items-center justify-center mx-auto mb-6">
                    <span class="font-bold text-2xl">1</span>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4">Install</h3>
                <p class="text-gray-600 text-lg">Add HarmonyUI to your Symfony project</p>
            </div>

            <div class="bg-white p-8 border border-gray-200 rounded-xl hover:border-primary hover:shadow-lg transition-all duration-300">
                <div class="w-16 h-16 bg-primary text-primary rounded-xl flex items-center justify-center mx-auto mb-6">
                    <span class="font-bold text-2xl">2</span>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4">Configure</h3>
                <p class="text-gray-600 text-lg">Set up your design system in YAML</p>
            </div>

            <div class="bg-white p-8 border border-gray-200 rounded-xl hover:border-primary hover:shadow-lg transition-all duration-300">
                <div class="w-16 h-16 bg-primary text-primary rounded-xl flex items-center justify-center mx-auto mb-6">
                    <span class="font-bold text-2xl">3</span>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4">Build</h3>
                <p class="text-gray-600 text-lg">Start creating maintainable interfaces</p>
            </div>
        </div>

        <div class="flex flex-col sm:flex-row gap-6 justify-center">
            <a href="{{ path('app_docs', {section: 'overview', page: 'getting-started'}) }}"
               class="inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-white bg-primary border border-transparent rounded-lg hover:bg-primary focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-all duration-200">
                <span>Get Started</span>
                <twig:ux:icon name="lucide:arrow-right" class="ml-2 w-5 h-5" />
            </a>

            <a href="{{ path('app_components') }}"
               class="inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-gray-700 bg-white border-2 border-gray-300 rounded-lg hover:border-primary hover:text-primary transition-all duration-200">
                <span>View Components</span>
                <twig:ux:icon name="lucide:eye" class="ml-2 w-5 h-5" />
            </a>
        </div>
    </div>
</section>
