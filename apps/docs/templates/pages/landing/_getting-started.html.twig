<section class="py-24 bg-gradient-to-br from-slate-50 to-gray-100">
    <div class="relative max-w-6xl mx-auto px-6 text-center">
        <h2 class="text-4xl font-bold text-gray-900 sm:text-5xl mb-8 text-balance">
            Get started in minutes
        </h2>

        <div class="mb-16 max-w-2xl mx-auto">
            <div class="bg-gray-900 border border-gray-700 rounded-xl p-8">
                <div class="flex items-center justify-between mb-4">
                    <span class="text-sm font-medium text-gray-400">Terminal</span>
                </div>
                <pre class="text-left text-lg"><code class="text-green-400 font-code">composer require stay/tuned</code></pre>
            </div>
        </div>

        <div class="flex flex-col sm:flex-row gap-6 justify-center">
            <twig:ui:Button
                tag="a"
                href="{{ path('app_docs', {section: 'overview', page: 'getting-started'}) }}"
                size="lg">
                <span>Get Started</span>
                <twig:ux:icon name="lucide:arrow-right" class="ml-2 w-5 h-5" />
            </twig:ui:Button>

            <twig:ui:Button
                tag="a"
                href="{{ path('app_components') }}"
                variant="outline"
                size="lg">
                <span>View Components</span>
                <twig:ux:icon name="lucide:eye" class="ml-2 w-5 h-5" />
            </twig:ui:Button>
        </div>
    </div>
</section>
